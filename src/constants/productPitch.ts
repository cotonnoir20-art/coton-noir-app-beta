/**
 * Textes alignés positionnement produit (copilote + soin des cheveux).
 * Tutoiement — à réutiliser dans l’app pour rester cohérent.
 */

/** Sous le salut sur l’accueil (une ligne sur mobile natif). */
export const HOME_TAGLINE =
  'Ton copilote capillaire — pour mieux prendre soin de tes cheveux.';

/** PWA / web : coupure manuelle pour éviter l’ellipse CSS (line-clamp). */
export const HOME_TAGLINE_WEB =
  'Ton copilote capillaire —\npour mieux prendre soin de tes cheveux.';

/** Sous-titre écran Récompenses. */
export const REWARDS_MONEY_LEGEND =
  'Les points cumulés font monter ton niveau. Les CotonCoins (CC) servent à échanger des récompenses.';

/** Sous le bloc « Moments forts » (carrousel). */
export const HIGHLIGHTS_PTS_LEGEND =
  'Les pts défi d’un challenge ne sont pas des CotonCoins (CC).';

/** Libellé court pour les scores de défi communauté / challenge. */
export const PTS_DEFI_LABEL = 'pts défi';

/** Hydra Challenge — gain journalier affiché (score défi, pas CC). */
export const CHALLENGE_HYDRA_PTS_PER_DAY = 30;

/** Sous la frise de paliers longueur (cm). */
export const GROWTH_MILESTONES_LEGEND =
  'Les paliers indiquent une longueur en cm, pas des CotonCoins.';

/** Sous-titre écran liste Moments forts / Découvrir (court). */
export const DISCOVER_LIST_SUBTITLE =
  'Outils, contenus et offres pour compléter ton parcours.';
